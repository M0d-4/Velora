# Copy this file to keystore.properties (same name, no .template)
# and fill in your values. This file is gitignored — never commit it.
#
# For CI signing, use GitHub Secrets instead (see .github/SETUP.md).

storeFile=../velora-release.jks
storePassword=your_keystore_password
keyAlias=velora
keyPassword=your_key_password
